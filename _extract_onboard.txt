function renderOnboard(){let s=ONBOARD[idx];$('onArt').innerHTML=`<img src="${asset(s.img)}" alt="${esc(s.title)}">`;$('onCopy').innerHTML=`<div class="progress"><span style="width:${((idx+1)/ONBOARD.length)*100}%"></span></div><div class="dots">${ONBOARD.map((_,i)=>`<span class="${i===idx?'active':''}"></span>`).join('')}</div><h2>${esc(s.title)}</h2><p class="muted">${esc(s.text)}</p><div class="actions"><button class="btn" onclick="prevOnboard()">Voltar</button><button class="btn primary" onclick="nextOnboard()">Próximo</button><button class="btn" onclick="closeOnboard(false)">Fechar</button></div><label style="margin-top:12px"><input type="checkbox" id="dontShow"> Não mostrar novamente</label><div class="countdown" id="onCount">Avança em 8s</div>`;clearInterval(onTimer);onLeft=8;onTimer=setInterval(()=>{onLeft--;$('onCount').textContent=`Avança em ${onLeft}s`;if(onLeft<=0)nextOnboard()},1000)}function nextOnboard(){idx=(idx+1)%ONBOARD.length;renderOnboard()}function prevOnboard(){idx=(idx-1+ONBOARD.length)%ONBOARD.length;renderOnboard()}function closeOnboard(force){if($('dontShow')?.checked||force)localStorage.setItem('gone_onboard_seen','1');$('onboard').classList.remove('open