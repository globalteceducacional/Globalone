function techCard(t){let feat=FEATURED_BY_GROUP[t.group_id]===t.id||PHOTO_HIGHLIGHT.includes(t.id);let sel=state.masterSelected.includes(t.id);let cost=canCost()?`<span class="tag green">Estimativa: ${money(breakdown(t).total)}</span>`:`<span class="tag red">Valores restritos</span>`;let cart=canCost()?`<button class="btn green" onclick="toggleCart('${t.id}')">${state.cart.includes(t.id)?'Remover do carrinho':'Adicionar ao carrinho'}</button>`:`<button class="btn" disabled>🔒 Carrinho restrito</button>`;return `<div class="card tech-card ${feat?'repo-highlight':''}"><div class="tech-img"><img src="${techImage(t)}" alt="${esc(t.name)}" onerror="this.onerror=null;this.src=placeholder('${esc(t.name)}')"></div><div class="body"><h3>${esc(t.name)}</h3><p class="muted desc">${esc(t.group||t.area||'Tecnologia aberta')} · ${esc(t.category||'')}</p><div class="toolbar"><span class="tag">${esc(t.id)}</span>${cost}${sel?'<span class="tag green">Selecionada pelo Master</span>':''}</div><div class="actions"><button class="btn" onclick="openTech('${t.id}')">Ver detalhes</button>${t.repo?`<a class="btn blue" href="${esc(t.repo)}" target="_blank" rel="noopener">Abrir repositório</a>`:''}<button cla