function renderRepositorio(){let groups=APP_DATA.portfolio.groups,campi=APP_DATA.portfolio.campi,list=filteredTechs();let pages=Math.max(1,Math.ceil(list.length/state.repo.size));state.repo.page=Math.min(state.repo.page,pages);let page=list.slice((state.repo.page-1)*state.repo.size,state.repo.page*state.repo.size);$('view-repositorio').innerHTML=`<div class="card"><h2>Repositório tecnológico G.one</h2><p class="muted">Tecnologias abertas com documentação, links, cursos, conversão em projeto e tutoria. Os primeiros cards priorizam tecnologias com fotos reais e pertinentes do produto — robôs, drones, veículos, aeronaves, CubeSat, aplicativos e eletrônicos — seguidas das demais bases tecnológicas. Quando não houver imagem confiável, aparece um marcador técnico institucional.</p><div class="toolbar"><input placeholder="Buscar tecnologia, área, código, repo..." value="${esc(state.repo.q)}" oninput="state.repo.q=this.value;state.repo.page=1;renderRepositorio()"><select onchange="state.repo.group=this.value;state.repo.page=1;renderRepositorio()"><option value="all">Todas as áreas</option>${groups.map(g=>`<option value="${g.id}" ${state.repo.group===g.id?'selected':''}>${esc(g.title)}</opt