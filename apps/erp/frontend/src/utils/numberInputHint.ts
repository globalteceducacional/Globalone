/**
 * Campos nativos `type="number"` seguem o padrão HTML: separador decimal é ponto (.),
 * não vírgula — útil em telas com teclado numérico e para evitar confusão em pt-BR.
 */
export const HTML_NUMBER_DECIMAL_HINT =
  'Decimais com ponto (ex.: 10.50), padrão dos campos numéricos do navegador.';
