name: gone-suite-local

services:
  gone:
    image: nginx:1.27-alpine
    container_name: gone-portal-local
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./apps/gone:/usr/share/nginx/html:ro
      - ./apps/gone_nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - suite-net

networks:
  suite-net:
    driver: bridge
