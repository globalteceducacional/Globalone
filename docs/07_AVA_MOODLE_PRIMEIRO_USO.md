# AVA/Moodle — primeiro uso

Local: `http://localhost:8081`. Produção: `https://ava.seudominio.com.br`.

O AVA usa MariaDB (`ava-db`) e volume `ava_moodledata`. Se o Moodle solicitar instalação inicial, use o host `ava-db` e dados do `.env`.
