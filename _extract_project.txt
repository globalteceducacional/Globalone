function projectCard(p,canEdit){return `<div class="card"><h3>${esc(p.title)}</h3><p class="muted">${esc(p.objective)}</p><div class="toolbar"><span class="tag green">${esc(p.status)}</span><span class="tag">Progresso ${p.progress||0}%</span>${canCost()?`<span class="tag green">${money(p.value||0)}</span>`:''}</div><div class="actions"><button class="btn" onclick="openProject('${p.id}')">Abrir demonstração completa</button><button class="btn primary" onclick="showView('ava')">Ver curso vinculado</button><button class="btn amber" onclick="deliverEvidence('${p.id}')">Entregar evidência</button>${canEdit?`<button class="btn" onclick="editProject('${p.id}')">Editar</button><button class="btn blue" onclick="downloadProject('${p.id}')">Exportar</button>`:''}</div></div>`}
function projectHTML(p)